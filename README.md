# StudentDataCrunchSQL README
-- SQL problems solved against a random generated set of data points.

CREATE TABLE IF NOT EXISTS cd.statcrunchsurvey
(
    gender character varying(6),
    class integer,
    hours integer,
    work integer,
    loans integer,
    ccdebt integer
)
